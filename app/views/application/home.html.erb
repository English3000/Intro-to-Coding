<%= javascript_pack_tag 'application' %>

<%= react_component 'Home', {}, prerender: true %>
<!-- The React component `Home` is converted here into HTML.
It is prerendered by the server (during the initial loading). -->
